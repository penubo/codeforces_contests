#include <iostream>
#include <algorithm>
#include <cstring>
#include <vector>

using namespace std;

int main() {

	int t;
	cin >> t;
	while (t--) {
		int n, m;
		cin >> n >> m;
		string x;
		vector<string> arr;
		for (int i = 0; i < n; i++) {
			cin >> x;
			arr.push_back(x);
		}

		
	}
	return 0;
}